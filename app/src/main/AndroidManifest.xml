<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.upnjatim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailDeveloper1" android:parentActivityName=".TentangActivity"/>
        <activity android:name=".DetailDeveloper" android:parentActivityName=".TentangActivity"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".TentangActivity" />
        <activity android:name=".KuisActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HasilKuis" />
    </application>

</manifest>